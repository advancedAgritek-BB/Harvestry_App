'use client';

import RecipeLayout from '@/features/dashboard/layouts/RecipeLayout';

export default function Layout({ children }: { children: React.ReactNode }) {
  return <RecipeLayout>{children}</RecipeLayout>;
}










